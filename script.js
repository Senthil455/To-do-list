(function () {   'use strict';    var STORAGE_KEY = 'todo_tasks';    var tasks = [];    var taskForm = document.getElementById('task-form');   var taskInput = document.getElementById('task-input');   var taskList = document.getElementById('task-list');    function loadTasks() {     try {       var stored = localStorage.getItem(STORAGE_KEY);       if (stored) {         tasks = JSON.parse(stored);       }     } catch (e) {       tasks = [];     }   }    function saveTasks() {     localStorage.setItem(STORAGE_KEY, JSON.stringify(tasks));   }    function renderTasks() {     taskList.innerHTML = '';     tasks.forEach(function (task, index) {       var li = document.createElement('li');       li.className = 'task-item' + (task.completed ? ' completed' : '');       li.innerHTML =         '<button class="btn-icon toggle-btn" data-index="' + index + '">' +         (task.completed ? '&#10003;' : '&#9673;') +         '</button>' +         '<span class="task-text">' + escapeHtml(task.title) + '</span>' +         '<button class="btn-icon delete-btn" data-index="' + index + '" title="Delete task">&#10005;</button>';       li.dataset.index = index;       taskList.appendChild(li);     });   }    function escapeHtml(text) {     var div = document.createElement('div');     div.appendChild(document.createTextNode(text));     return div.innerHTML;   }    function handleAddTask(e) {     e.preventDefault();     var title = taskInput.value.trim();     if (!title) {       showValidationError();       return;     }     tasks.push({ id: Date.now(), title: title, completed: false });     saveTasks();     renderTasks();     taskInput.value = '';     taskInput.focus();   }    function handleTaskClick(e) {     var toggleBtn = e.target.closest('.toggle-btn');     if (toggleBtn) {       var index = parseInt(toggleBtn.dataset.index, 10);       tasks[index].completed = !tasks[index].completed;       saveTasks();       renderTasks();       return;     }      var deleteBtn = e.target.closest('.delete-btn');     if (deleteBtn) {       var idx = parseInt(deleteBtn.dataset.index, 10);       if (confirm('Delete this task?')) {         tasks.splice(idx, 1);         saveTasks();         renderTasks();       }     }   }    function showValidationError() {     taskInput.classList.add('input-error');     taskInput.placeholder = 'Please enter a task name';     setTimeout(function () {       taskInput.classList.remove('input-error');       taskInput.placeholder = 'Enter a new task...';     }, 2000);   }    function init() {     loadTasks();     renderTasks();     taskForm.addEventListener('submit', handleAddTask);     taskList.addEventListener('click', handleTaskClick);   }    document.addEventListener('DOMContentLoaded', init); })();